name: Classic CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  # ==================== CI PHASE ====================
  ci-test:
    name: 🔍 CI - Test Code
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Node.js 18
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: npm install
      
    - name: Run tests
      run: npm test

  # ==================== CD PHASE ====================
  cd-deploy:
    name: 🚀 CD - Deploy to VM
    runs-on: ubuntu-latest
    needs: ci-test
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    # DEMO: This step would deploy to a cloud VM in production
    # For workshop demo, this will timeout (expected behavior)
    - name: Deploy files via SSH
      uses: easingthemes/ssh-deploy@v5.0.0
      with:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        REMOTE_HOST: ${{ secrets.HOST }}
        REMOTE_USER: ${{ secrets.USERNAME }}
        REMOTE_PORT: 22
        SOURCE: "/"
        TARGET: "app"
        ARGS: "-rlgoDzvc -i --delete"
        SCRIPT_AFTER: "cd app && npm install && pm2 restart app || pm2 start src/app.js --name app"
  cd-verify:
    name: ✅ CD - Verify Success
    runs-on: ubuntu-latest
    needs: cd-deploy
    
    steps:
    - name: Pipeline Summary
      run: |
        echo "🔍 CI Phase: Code tested & validated"
        echo "🚀 CD Phase: Files deployed to VM"
        echo "✅ Classic CI/CD Pipeline Complete!"